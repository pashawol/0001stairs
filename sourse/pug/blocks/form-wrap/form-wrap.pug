mixin form-head()
	form.js-form-validate( data-parsley-validate="") 
		input(type='hidden', class="order", name='order', value=`Заявка  с сайта` ) 
		block

mixin input(placeholder,  t='text', label, name= t)
	mixin t(t='text',placeholder, name= t)
		if t== "textarea"
			+e.TEXTAREA.input.form-control(placeholder= placeholder name= name)&attributes(attributes)
		else if t=="select"
			+e.SELECT.input.form-control()&attributes(attributes)
					option()= placeholder 
					- for (var x = 1; x < 4; x++)
						option()= placeholder +' ' +  x
						option()= placeholder +' ' +  x
						option()= placeholder +' ' +  x
		else
			+e.INPUT.input--sm(type= t placeholder= placeholder name= name)&attributes(attributes)
	+e.input-wrap--sm
		if label
			label
				+e.SPAN.title--sm= label
				+t(t,placeholder, name)&attributes(attributes)
				block
		else
			+t(t,placeholder, name)&attributes(attributes)
		block
	// +e.input-wrap
	
mixin input-lg(placeholder,  t='text', label, name= t)
	mixin t(t='text',placeholder, name= t)
		if t== "textarea"
			+e.TEXTAREA.input--lg.form-control(placeholder= placeholder name= name)&attributes(attributes)
		else
			+e.INPUT.input--lg(type= t placeholder= placeholder name= name)&attributes(attributes)
	+e.input-wrap--lg
		if label
			label
				+e.SPAN.title= label
				+t(t,placeholder, name)&attributes(attributes)
				block
		else
			+t(t,placeholder, name)&attributes(attributes)
		block
	// +e.input-wrap

mixin select(label,pl,...items)
	+e.input-wrap
		label
			+e.SPAN.title= label
			+e.SELECT.input

					option()= pl
					each item in items
						option()= item

//- wrapper for form
mixin form-wrap()
	+b.form-wrap&attributes(attributes)
		form
			input.order(type="hidden" name="order")
			block
//- /wrapper for form


mixin in-c(typ="checkbox", text, name= typ )
	+b.LABEL.custom-input
		+e.INPUT.input(type= typ , name= name)&attributes(attributes)
		+e.SPAN.lab
		if block
			block 
		else
			+e.SPAN.text= text
 
mixin add-file(n)
	+b.add-file
		label
			+e.INPUT.input(name='file', type='file')
			+e.SPAN.btn 
			+e.SPAN.text   Прикрепить резюме
		+e.SPAN.filename 


mixin polite()
	+e.polite
		+in-c()(checked)
			+e.SPAN.text Я согласен(а) с условиями передачи информации